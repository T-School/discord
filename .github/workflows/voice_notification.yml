name: voice_notification
on:
  # 手動での実行
  workflow_dispatch:
  # 毎日0:00、6:00、12:00、18:00に定期実行（表記はUTC）
  schedule:
    - cron: "0 3,9,15,21 * * *"
# 稼働中の同一ワークフローがあれば停止させる
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: Create dot env file
        run: |
          touch .env
          echo "TOKEN=${{ secrets.TOKEN }}" >> .env
          echo "VOICE_CHANNEL_ID=${{ secrets.VOICE_CHANNEL_ID }}" >> .env
          echo "MESSAGE_CHANNEL_ID=${{ secrets.MESSAGE_CHANNEL_ID }}" >> .env
          echo "TIMEOUT_SECONDS=${{ secrets.TIMEOUT_SECONDS }}" >> .env
      - run: npm install
      - name: Start Watching Voice Channel
        # continue-on-error: true # エラーが起きても処理を続行
        run: node src/voiceMemberNotification.js
      - name: Success Exit even if cancelled
        if: cancelled()
        run: exit 0
      - name: Error Messege
        if: error()
        run: node src/sendMessage.js "サーバ側でエラー発生。Botを停止します😴\n最大で6時間以内に自動で再起動します💡"
        timeout-minutes: 1
